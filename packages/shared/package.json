{
  "name": "@worktime/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and utilities for WorkTime",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:dev": "tsc --watch",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint 'src/**/*.ts'",
    "lint:fix": "eslint 'src/**/*.ts' --fix"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
