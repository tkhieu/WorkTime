name = "worktime-backend"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Account details (to be configured)
# account_id = "YOUR_CLOUDFLARE_ACCOUNT_ID"

# Workers settings
workers_dev = true

# Environment variables (development)
[env.development]
name = "worktime-backend-dev"

# Environment variables (production)
[env.production]
name = "worktime-backend-prod"

# D1 Database binding (to be configured in Phase 08)
# [[d1_databases]]
# binding = "DB"
# database_name = "worktime-db"
# database_id = "YOUR_DATABASE_ID"

# KV namespace for token storage (to be configured in Phase 08)
# [[kv_namespaces]]
# binding = "TOKENS"
# id = "YOUR_KV_ID"

# R2 bucket for file storage (optional)
# [[r2_buckets]]
# binding = "FILES"
# bucket_name = "worktime-files"
